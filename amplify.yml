version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm --prefix ui ci
    build:
      commands:
        - npm --prefix ui run build
  artifacts:
    baseDirectory: ui/.next
    files:
      - '**/*'
  cache:
    paths:
      - ui/node_modules/**/*
